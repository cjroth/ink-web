---
title: Vite (Plugin)
description: Advanced setup using the Vite plugin with standalone ink
---

This is an advanced setup that uses the Vite plugin to transform the real `ink` package for browser use. This results in a smaller bundle size but requires additional configuration.

For most users, we recommend the simpler [Vite setup](/docs/installation/vite) which uses the pre-bundled version.

import { Tabs, Tab } from 'fumadocs-ui/components/tabs';

<Tabs items={['npm', 'yarn', 'pnpm', 'bun']}>
  <Tab value="npm">
    ```bash
    npm install ink-web ink ghostty-web
    ```
  </Tab>
  <Tab value="yarn">
    ```bash
    yarn add ink-web ink ghostty-web
    ```
  </Tab>
  <Tab value="pnpm">
    ```bash
    pnpm add ink-web ink ghostty-web
    ```
  </Tab>
  <Tab value="bun">
    ```bash
    bun add ink-web ink ghostty-web
    ```
  </Tab>
</Tabs>


**Vite Configuration:**

```tsx
import { inkWebPlugin } from 'ink-web/vite'
import react from '@vitejs/plugin-react'
import { defineConfig } from 'vite'

export default defineConfig({
  plugins: [react(), inkWebPlugin()],
})
```

**Usage:**

```tsx
import { Box, Text } from "ink";
import { InkXterm } from "ink-web/core";

const App = () => (
  <div style={{ width: "100vw", height: "100vh" }}>
    <InkXterm focus>
      <Box flexDirection="column">
        <Text color="green">Hello from Ink!</Text>
      </Box>
    </InkXterm>
  </div>
);

export default App;
```

With this setup, you import Ink components from `ink` and wrapper components from `ink-web/core`. The Vite plugin handles transforming the `ink` imports for browser compatibility.
