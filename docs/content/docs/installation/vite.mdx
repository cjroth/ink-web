---
title: Vite
---
Start with the [example](https://github.com/cjroth/ink-web/tree/main/examples/vite) or follow this guide:

import { Tabs, Tab } from 'fumadocs-ui/components/tabs';

<Tabs items={['npm', 'yarn', 'pnpm', 'bun']}>
  <Tab value="npm">
    ```bash
    npm install ink-web ink xterm
    ```
  </Tab>
  <Tab value="yarn">
    ```bash
    yarn add ink-web ink xterm
    ```
  </Tab>
  <Tab value="pnpm">
    ```bash
    pnpm add ink-web ink xterm
    ```
  </Tab>
  <Tab value="bun">
    ```bash
    bun add ink-web ink xterm
    ```
  </Tab>
</Tabs>

**Usage:**

```tsx
import { Box, Text } from "ink";
import { InkXterm } from "ink-web/bundled";
import "xterm/css/xterm.css";

const App = () => (
  <div style={{ width: "100vw", height: "100vh" }}>
    <InkXterm focus>
      <Box flexDirection="column">
        <Text color="green">Hello from Ink!</Text>
      </Box>
    </InkXterm>
  </div>
);

export default App;
```

Note: Import ink components from `ink` separately.
